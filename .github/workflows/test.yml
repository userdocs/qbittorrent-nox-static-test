name: test
on:
  workflow_dispatch:
  push:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-1:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          curl --version

  test-2:
    runs-on: ubuntu-latest
    steps:
      - name: test
        shell: bash {0}
        run: |
          git --version
